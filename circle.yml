machine:
  ruby:
    version: 2.3.0

dependencies:
  pre:
    - gem install bundler

test:
  override:
    - bundle exec rspec --format JUnit --out $CIRCLE_TEST_REPORTS/rspec.xml:
        parallel: true
        files:
          - spec/*/*_spec.rb
