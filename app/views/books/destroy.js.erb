<% if @book.errors.any? %>
alert('<%= t('book.error.destroy') %>' + '<%= @book.errors.full_messages.join('\n').html_safe %>');
<% else %>
$('tr#book-row-<%= @book.id %>').fadeOut(500, function () {
  $(this).remove()
});
<% end %>